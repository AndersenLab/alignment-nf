# Build dockerfile on change
name: Build Docker (env/blobtools.Dockerfile)

on:
  push:
    paths:
    - 'env/blobtools.Dockerfile'
    - '.github/workflows/build_blobdocker.yml'
  pull_request:
    paths:
    - 'env/blobtools.Dockerfile'
    - '.github/workflows/build_blobdocker.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2

    # Build Tools
    - name: Build and Publish
      uses: elgohr/Publish-Docker-Github-Action@master
      with:
        name: andersenlab/blobtools
        tag: "${{ steps.current-time.formattedTime }}"
        username: ${{ secrets.KSE_DOCKER_USER }}
        password: ${{ secrets.KSE_DOCKER_PASS }}
        snapshot: true
        dockerfile: blobtools.Dockerfile
        workdir: "env"
        tags: "latest"
        cache: true 